require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe PersonalAwardsController do

  # This should return the minimal set of attributes required to create a valid
  # PersonalAward. As you add validations to PersonalAward, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) { { "name" => "MyString" } }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # PersonalAwardsController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET index" do
    it "assigns all personal_awards as @personal_awards" do
      personal_award = PersonalAward.create! valid_attributes
      get :index, {}, valid_session
      assigns(:personal_awards).should eq([personal_award])
    end
  end

  describe "GET show" do
    it "assigns the requested config_person as @config_person" do
      personal_award = PersonalAward.create! valid_attributes
      get :show, {:id => personal_award.to_param}, valid_session
      assigns(:config_person).should eq(personal_award)
    end
  end

  describe "GET new" do
    it "assigns a new config_person as @config_person" do
      get :new, {}, valid_session
      assigns(:config_person).should be_a_new(PersonalAward)
    end
  end

  describe "GET edit" do
    it "assigns the requested config_person as @config_person" do
      personal_award = PersonalAward.create! valid_attributes
      get :edit, {:id => personal_award.to_param}, valid_session
      assigns(:config_person).should eq(personal_award)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new PersonalAward" do
        expect {
          post :create, {:config_person => valid_attributes}, valid_session
        }.to change(PersonalAward, :count).by(1)
      end

      it "assigns a newly created config_person as @config_person" do
        post :create, {:config_person => valid_attributes}, valid_session
        assigns(:config_person).should be_a(PersonalAward)
        assigns(:config_person).should be_persisted
      end

      it "redirects to the created config_person" do
        post :create, {:config_person => valid_attributes}, valid_session
        response.should redirect_to(PersonalAward.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved config_person as @config_person" do
        # Trigger the behavior that occurs when invalid params are submitted
        PersonalAward.any_instance.stub(:save).and_return(false)
        post :create, {:config_person => { "name" => "invalid value" }}, valid_session
        assigns(:config_person).should be_a_new(PersonalAward)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        PersonalAward.any_instance.stub(:save).and_return(false)
        post :create, {:config_person => { "name" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested config_person" do
        personal_award = PersonalAward.create! valid_attributes
        # Assuming there are no other personal_awards in the database, this
        # specifies that the PersonalAward created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        PersonalAward.any_instance.should_receive(:update).with({ "name" => "MyString" })
        put :update, {:id => personal_award.to_param, :config_person => { "name" => "MyString" }}, valid_session
      end

      it "assigns the requested config_person as @config_person" do
        personal_award = PersonalAward.create! valid_attributes
        put :update, {:id => personal_award.to_param, :config_person => valid_attributes}, valid_session
        assigns(:config_person).should eq(personal_award)
      end

      it "redirects to the config_person" do
        personal_award = PersonalAward.create! valid_attributes
        put :update, {:id => personal_award.to_param, :config_person => valid_attributes}, valid_session
        response.should redirect_to(personal_award)
      end
    end

    describe "with invalid params" do
      it "assigns the config_person as @config_person" do
        personal_award = PersonalAward.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        PersonalAward.any_instance.stub(:save).and_return(false)
        put :update, {:id => personal_award.to_param, :config_person => { "name" => "invalid value" }}, valid_session
        assigns(:config_person).should eq(personal_award)
      end

      it "re-renders the 'edit' template" do
        personal_award = PersonalAward.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        PersonalAward.any_instance.stub(:save).and_return(false)
        put :update, {:id => personal_award.to_param, :config_person => { "name" => "invalid value" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested config_person" do
      personal_award = PersonalAward.create! valid_attributes
      expect {
        delete :destroy, {:id => personal_award.to_param}, valid_session
      }.to change(PersonalAward, :count).by(-1)
    end

    it "redirects to the personal_awards list" do
      personal_award = PersonalAward.create! valid_attributes
      delete :destroy, {:id => personal_award.to_param}, valid_session
      response.should redirect_to(personal_awards_url)
    end
  end

end
