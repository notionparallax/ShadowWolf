<%= form_for(@security_clearance) do |f| %>
  <% if @security_clearance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@security_clearance.errors.count, "error") %> prohibited this security_clearance from being saved:</h2>

      <ul>
      <% @security_clearance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :issuing_body %><br>
    <%= f.text_field :issuing_body %>
  </div>
  <div class="field">
    <%= f.label :level %><br>
    <%= f.text_field :level %>
  </div>
  <div class="field">
    <%= f.label :expiry %><br>
    <%= f.date_select :expiry %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
