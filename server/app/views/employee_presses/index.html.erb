<h1>Listing employee_presses</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @employee_presses.each do |employee_press| %>
      <tr>
        <td><%= link_to 'Show', employee_press %></td>
        <td><%= link_to 'Edit', edit_employee_press_path(employee_press) %></td>
        <td><%= link_to 'Destroy', employee_press, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Employee press', new_employee_press_path %>
