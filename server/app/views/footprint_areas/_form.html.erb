<%= form_for(@footprint_area) do |f| %>
  <% if @footprint_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@footprint_area.errors.count, "error") %> prohibited this footprint_area from being saved:</h2>

      <ul>
      <% @footprint_area.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :measurement %><br>
    <%= f.text_field :measurement %>
  </div>
  <div class="field">
    <%= f.label :standard_name %><br>
    <%= f.text_field :standard_name %>
  </div>
  <div class="field">
    <%= f.label :standard_version %><br>
    <%= f.text_field :standard_version %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
