<%= form_for(@passport) do |f| %>
  <% if @passport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@passport.errors.count, "error") %> prohibited this passport from being saved:</h2>

      <ul>
      <% @passport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :expiry %><br>
    <%= f.date_select :expiry %>
  </div>
  <div class="field">
    <%= f.label :issuing_body %><br>
    <%= f.text_field :issuing_body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
