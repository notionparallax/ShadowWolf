<h1>Listing dimension_groups</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @dimension_groups.each do |dimension_group| %>
      <tr>
        <td><%= link_to 'Show', dimension_group %></td>
        <td><%= link_to 'Edit', edit_dimension_group_path(dimension_group) %></td>
        <td><%= link_to 'Destroy', dimension_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dimension group', new_dimension_group_path %>
